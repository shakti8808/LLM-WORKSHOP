## 1. Backend Integration

- [x] 1.1 Update task creation and serialization logic in `todo.py` to store and load task category tags

## 2. Frontend HTML & CSS

- [x] 2.1 Add category selection dropdown and dynamic filter pills bar to `templates/index.html`
- [x] 2.2 Implement style rules for category dropdowns, filter bars, and color-coded tag badges in `static/style.css`

## 3. JavaScript Logic & Interaction

- [x] 3.1 Update task creation, toggling, and rendering in `static/app.js` to handle categories and dynamic rendering
- [x] 3.2 Implement client-side filtering on category pill click, updating task counters and progress bars
- [x] 3.3 Ensure the offline `localStorage` fallback stores categories and handles local filtering correctly

## 4. Verification

- [x] 4.1 Add automated test cases to `test_todo.py` verifying category persistence
